[project]
name = "project-os"
version = "0.1.0"
description = "A minimal, auditable Evolution Agent for personal websites"
requires-python = ">=3.11"
dependencies = ["langgraph>=0.2,<1"]

[project.scripts]
project-os = "project_os.cli:main"

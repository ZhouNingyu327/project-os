[project]
name = "project-os"
version = "0.2.0"
description = "An auditable, policy-controlled Evolution Agent for personal websites"
requires-python = ">=3.11"
dependencies = ["langgraph>=0.2,<1"]

[project.scripts]
project-os = "project_os.cli:main"
